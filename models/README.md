Place the L/R model weights here so the viewer can classify Left/Right from thumbnails:

  left_right_mobilenetv3.pth

Copy it from the main project: ML_Left_Right/left_right_mobilenetv3.pth

If this file is missing, the viewer still runs but shows "?" for L/R on unsorted videos.
